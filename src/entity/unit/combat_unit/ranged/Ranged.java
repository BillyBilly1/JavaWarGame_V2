package entity.unit.combat_unit.ranged;

import entity.unit.combat_unit.CombatUnit;

public class Ranged extends CombatUnit {
    public Ranged(int x, int y, boolean isLeft) {
        super(x, y, isLeft);
    }
}
