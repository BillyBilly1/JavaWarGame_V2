package entity.unit.combat_unit.melee;

import entity.unit.combat_unit.CombatUnit;

public class Melee extends CombatUnit {
    public Melee(int x, int y, boolean isLeft) {
        super(x, y, isLeft);
    }
}
